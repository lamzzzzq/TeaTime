# 后端服务器配置
NODE_ENV=production
PORT=3000
HOST=0.0.0.0

# CORS配置 - 允许访问的前端域名
# 开发环境可以使用 * ，生产环境建议指定具体域名
CORS_ORIGIN=*

# 可选：数据库配置（如果将来需要）
# DATABASE_URL=

# 可选：日志级别
LOG_LEVEL=info

# 可选：会话密钥（如果将来需要用户会话）
# SESSION_SECRET=your-secret-key-here

# Unity WebGL 配置
UNITY_BUILD_PATH=/unity-build

# 可选：CDN配置（如果使用CDN托管Unity文件）
# CDN_BASE_URL=https://your-cdn.com
